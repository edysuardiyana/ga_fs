import numpy as np
from sklearn.preprocessing import Imputer
import statistics as st

#x = [[1],[2],[3],[4],['NaN'],[6],[7],['NaN'], ['NaN']]
#x_new = np.array(x)

#print x_new
#imp = Imputer(missing_values='NaN', strategy='median', axis=0)
#x_new = imp.fit_transform(x_new)

#print x_new


for i in range(-2):
    print "hi"
